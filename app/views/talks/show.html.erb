<%= @talk.topic %> <%= link_to "Edit Talk", edit_talk_path %> <%= link_to "Delete Talk", talk_path, method: :delete %>
<%= @comments.each do |comment| %>
  <%= comment.content %>
<%-end-%>

<%= render 'comments/form' %>

